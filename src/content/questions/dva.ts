import type { Question } from '../schema'

/**
 * DVA-C02. A build-and-debug paper, not an architecture paper — it wants the
 * specific setting, the specific API call, the specific error. Lambda and
 * DynamoDB carry most of the weight.
 */
export const dvaQuestions: Question[] = [
  /* ── Domain 1: Development with AWS Services (32%) ────────────────────── */
  {
    id: 'dva-d1-001',
    certs: ['DVA-C02'],
    taskId: 'dva-1.2',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['lambda'],
    stem: 'A Lambda function opens a database connection on every invocation and is exhausting the connection pool under load. Where should the connection be created?',
    options: [
      { id: 'A', text: 'Outside the handler, in the initialisation code, so it is reused across invocations in the same execution environment.', correct: true, why: 'Initialisation code runs once per execution environment, not once per invocation, so warm invocations reuse the connection. This is asked directly on DVA.' },
      { id: 'B', text: 'Inside the handler, but closed in a finally block.', correct: false, why: 'It avoids leaks but still pays connection setup on every single invocation, which is the cost being complained about.' },
      { id: 'C', text: 'In a Lambda layer, so it is shared between functions.', correct: false, why: 'Layers ship code and dependencies. They do not share runtime state between functions.' },
      { id: 'D', text: 'In a Lambda extension running as a separate process.', correct: false, why: 'Extensions are for telemetry and lifecycle hooks, not for sharing application database connections.' },
    ],
    explanation:
      'Anything expensive and reusable — SDK clients, connection pools, parsed configuration — belongs outside the handler. For relational databases, add RDS Proxy as well.',
    source: 'https://docs.aws.amazon.com/lambda/latest/dg/best-practices.html',
  },
  {
    id: 'dva-d1-002',
    certs: ['DVA-C02'],
    taskId: 'dva-1.3',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['dynamodb'],
    stem: 'A DynamoDB Query returns 40 items when the developer expects 900. No error is raised. What is the most likely cause?',
    options: [
      { id: 'A', text: 'The 1 MB result-page limit was reached and the code is not following LastEvaluatedKey to fetch subsequent pages.', correct: true, why: 'Query returns at most 1 MB per call and signals more results with LastEvaluatedKey. Ignoring it silently truncates the result set.' },
      { id: 'B', text: 'The table has insufficient read capacity, so items were dropped.', correct: false, why: 'Insufficient capacity produces a ProvisionedThroughputExceededException, not silently missing items.' },
      { id: 'C', text: 'Eventually consistent reads returned stale data.', correct: false, why: 'Eventual consistency can return slightly stale values, not a truncated count of this magnitude.' },
      { id: 'D', text: 'The FilterExpression removed the other items before they were read.', correct: false, why: 'A filter is applied after the read, and it would explain fewer items only if a filter were present — the far more common cause is pagination.' },
    ],
    explanation:
      'Every DynamoDB read API paginates at 1 MB. Use the SDK paginators, or loop on LastEvaluatedKey — "fewer results than expected" is nearly always this.',
    source: 'https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Query.html',
  },
  {
    id: 'dva-d1-003',
    certs: ['DVA-C02'],
    taskId: 'dva-1.3',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['dynamodb'],
    stem: 'Two clients read the same DynamoDB item and both write an update. One update is silently lost. Which mechanism prevents this?',
    options: [
      { id: 'A', text: 'A conditional write using a version attribute — optimistic locking.', correct: true, why: 'A ConditionExpression asserting the version has not changed makes the second write fail rather than overwrite, and the client can retry with fresh data.' },
      { id: 'B', text: 'Strongly consistent reads on both clients.', correct: false, why: 'It guarantees each client reads the latest value, but does nothing to stop the second write overwriting the first.' },
      { id: 'C', text: 'A DynamoDB transaction wrapping each individual write.', correct: false, why: 'Transactions give atomicity across items. A single-item write is already atomic — the problem is the lost update, not partial writes.' },
      { id: 'D', text: 'Enabling DynamoDB Streams to detect the conflict afterwards.', correct: false, why: 'Streams let you observe what happened; they do not prevent it.' },
    ],
    explanation:
      'Optimistic locking with a version attribute plus a ConditionExpression is the standard DynamoDB answer to concurrent updates.',
    source: 'https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/DynamoDBMapper.OptimisticLocking.html',
  },
  {
    id: 'dva-d1-004',
    certs: ['DVA-C02'],
    taskId: 'dva-1.2',
    type: 'single',
    difficulty: 3,
    serviceSlugs: ['lambda'],
    stem: 'A Lambda function has reserved concurrency of 5. During a traffic burst, callers receive 429 TooManyRequestsException. The account concurrency limit is 1,000 and is not near being reached. What explains this?',
    options: [
      { id: 'A', text: 'Reserved concurrency also acts as a maximum, capping this function at 5 concurrent executions.', correct: true, why: 'Reserved concurrency does two things at once: it guarantees that much capacity and it caps the function at exactly that number. This dual behaviour is examined directly.' },
      { id: 'B', text: 'Reserved concurrency only guarantees a minimum, so the cause must be the account limit.', correct: false, why: 'It is a hard ceiling as well as a floor, and the question states the account limit is not the constraint.' },
      { id: 'C', text: 'Provisioned concurrency needs to be enabled to remove throttling.', correct: false, why: 'Provisioned concurrency pre-warms environments to remove cold starts. It does not raise the reserved ceiling.' },
      { id: 'D', text: 'The function timeout is too short, so invocations are being rejected.', correct: false, why: 'A short timeout produces a task-timeout error inside the invocation, not a 429 at the API boundary.' },
    ],
    explanation:
      'Reserved concurrency = guarantee *and* cap. Provisioned concurrency = pre-warmed environments to remove cold starts. Two different settings, constantly confused.',
    source: 'https://docs.aws.amazon.com/lambda/latest/dg/configuration-concurrency.html',
  },
  {
    id: 'dva-d1-005',
    certs: ['DVA-C02'],
    taskId: 'dva-1.2',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['lambda', 'sqs'],
    stem: 'A Lambda function is invoked asynchronously by S3 events. Some events appear to be lost when the function throws an error. What should be configured to capture them?',
    options: [
      { id: 'A', text: 'An on-failure destination or a dead-letter queue on the function.', correct: true, why: 'Asynchronous invocations retry twice more and then discard the event unless a DLQ or on-failure destination is configured to receive it.' },
      { id: 'B', text: 'Increase the function timeout so it has more time to succeed.', correct: false, why: 'The function is erroring, not timing out. The event is still discarded after retries.' },
      { id: 'C', text: 'Enable S3 versioning so the event can be replayed.', correct: false, why: 'Versioning preserves objects, not the notification events derived from them.' },
      { id: 'D', text: 'Switch to synchronous invocation so the caller can retry.', correct: false, why: 'S3 event notifications to Lambda are asynchronous; this is not a configurable choice.' },
    ],
    explanation:
      'Async: two extra retries, then a DLQ or destination or the event is gone. Sync: no retries at all — the caller owns the retry.',
    source: 'https://docs.aws.amazon.com/lambda/latest/dg/invocation-async.html',
  },
  {
    id: 'dva-d1-006',
    certs: ['DVA-C02'],
    taskId: 'dva-1.1',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['sdk', 'dynamodb'],
    stem: 'An application calling DynamoDB intermittently receives ProvisionedThroughputExceededException. What is the appropriate first response in the application code?',
    options: [
      { id: 'A', text: 'Rely on the SDK\'s built-in exponential backoff with jitter, and raise the retry configuration if needed.', correct: true, why: 'The AWS SDKs already implement exponential backoff with jitter for throttling errors. Hand-rolling it is the wrong answer; tuning maxAttempts or the retry mode is right.' },
      { id: 'B', text: 'Write a custom retry loop with a fixed 100 ms delay.', correct: false, why: 'A fixed delay from many clients synchronises retries and makes throttling worse. Jitter exists specifically to avoid this.' },
      { id: 'C', text: 'Catch the exception and return an error to the user immediately.', correct: false, why: 'Throttling is transient by nature, so failing straight through discards a request that would likely succeed on retry.' },
      { id: 'D', text: 'Switch every read to eventually consistent to halve the capacity used.', correct: false, why: 'Sometimes a valid optimisation, but it changes consistency semantics and does not address transient throttling properly.' },
    ],
    explanation:
      'The SDKs retry throttling errors with exponential backoff and jitter by default. Answers that propose writing your own are usually wrong on DVA.',
    source: 'https://docs.aws.amazon.com/sdkref/latest/guide/feature-retry-behavior.html',
  },
  {
    id: 'dva-d1-007',
    certs: ['DVA-C02'],
    taskId: 'dva-1.2',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['lambda', 'vpc', 'privatelink'],
    stem: 'A Lambda function is configured for VPC access so it can reach an RDS instance in a private subnet. It now times out calling the public Stripe API. What is the cause and the cheapest fix for AWS API calls made by the same function?',
    options: [
      { id: 'A', text: 'A private subnet has no internet route, so the function needs a NAT gateway for third-party APIs — and VPC interface endpoints are cheaper for AWS service calls.', correct: true, why: 'Attaching a function to a VPC removes its default internet access. Third-party APIs need NAT; AWS APIs are better served by endpoints, which avoid the per-GB NAT charge.' },
      { id: 'B', text: 'The function needs a public IP address assigned.', correct: false, why: 'Lambda ENIs cannot be given public IP addresses. Egress must go through NAT.' },
      { id: 'C', text: 'The function\'s security group needs an inbound rule for the Stripe API.', correct: false, why: 'Outbound calls need an outbound rule, and security groups are stateful so the reply is automatic. This is a routing problem, not a filtering one.' },
      { id: 'D', text: 'The function should be removed from the VPC entirely.', correct: false, why: 'It would restore internet access but break the private RDS connection the VPC attachment exists for.' },
    ],
    explanation:
      'A VPC-attached Lambda has private-subnet networking, with all that implies. This is one of the most common real-world Lambda surprises and a recurring exam item.',
    source: 'https://docs.aws.amazon.com/lambda/latest/dg/foundation-networking.html',
  },
  {
    id: 'dva-d1-008',
    certs: ['DVA-C02'],
    taskId: 'dva-1.1',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['api-gateway', 'lambda'],
    stem: 'An API Gateway REST API with Lambda proxy integration returns HTTP 502 Bad Gateway. The function logs show it completed successfully. What is the most likely cause?',
    options: [
      { id: 'A', text: 'The function is not returning the response shape Lambda proxy integration requires, with statusCode and body.', correct: true, why: 'Proxy integration requires a specific JSON envelope. A function that returns a bare object succeeds internally, and API Gateway cannot translate it — producing a 502.' },
      { id: 'B', text: 'The function exceeded the 29-second integration timeout.', correct: false, why: 'That produces a 504 Gateway Timeout, not a 502.' },
      { id: 'C', text: 'API Gateway is being throttled.', correct: false, why: 'Throttling returns 429 Too Many Requests.' },
      { id: 'D', text: 'The API Gateway execution role lacks lambda:InvokeFunction.', correct: false, why: 'That produces a 500 with an authorisation error, and the function would never run at all.' },
    ],
    explanation:
      'Know the codes: 502 malformed response, 504 integration timeout, 429 throttling, 403 authoriser rejection. DVA asks these directly.',
    source: 'https://docs.aws.amazon.com/apigateway/latest/developerguide/set-up-lambda-proxy-integrations.html',
  },
  {
    id: 'dva-d1-009',
    certs: ['DVA-C02'],
    taskId: 'dva-1.3',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['dynamodb'],
    stem: 'A table must automatically delete session records 30 days after creation, at no additional cost. What should be used?',
    options: [
      { id: 'A', text: 'Enable TTL on an attribute holding the expiry time as a Unix epoch timestamp.', correct: true, why: 'TTL deletes expired items with no write capacity consumed and no cost. Deletion happens within about 48 hours of expiry rather than instantly.' },
      { id: 'B', text: 'A scheduled Lambda function scanning for old items and deleting them.', correct: false, why: 'It works but consumes read and write capacity, and a full-table scan is exactly the access pattern to avoid.' },
      { id: 'C', text: 'A DynamoDB Streams consumer that deletes items after 30 days.', correct: false, why: 'Streams have a 24-hour retention, so a 30-day delay cannot be driven from them.' },
      { id: 'D', text: 'An S3 lifecycle rule on the table\'s backups.', correct: false, why: 'Lifecycle rules apply to S3 objects, not DynamoDB items.' },
    ],
    explanation:
      'TTL is free and the intended answer for expiring data. Note that deletion is eventual — filter expired items in queries if precision matters.',
    source: 'https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/TTL.html',
  },
  {
    id: 'dva-d1-010',
    certs: ['DVA-C02'],
    taskId: 'dva-1.1',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['sqs', 'lambda'],
    stem: 'A Lambda function processes SQS messages in batches of 10. When one message in a batch fails, all 10 are redelivered and the successful nine are processed again. What should be configured?',
    options: [
      { id: 'A', text: 'Enable ReportBatchItemFailures on the event source mapping and return the failed message identifiers from the handler.', correct: true, why: 'Partial batch response makes only the failed messages become visible again, so successful ones are not reprocessed.' },
      { id: 'B', text: 'Reduce the batch size to 1.', correct: false, why: 'It solves the symptom by giving up batching entirely, raising invocation count and cost.' },
      { id: 'C', text: 'Increase the visibility timeout.', correct: false, why: 'That addresses duplicate processing from timeouts, not partial batch failure.' },
      { id: 'D', text: 'Switch to a FIFO queue.', correct: false, why: 'FIFO makes it worse — a failing message blocks its entire message group.' },
    ],
    explanation:
      'ReportBatchItemFailures is the specific fix for partial batch failure, and it applies to SQS, Kinesis and DynamoDB Streams event sources.',
    source: 'https://docs.aws.amazon.com/lambda/latest/dg/with-sqs.html',
  },
  {
    id: 'dva-d1-011',
    certs: ['DVA-C02'],
    taskId: 'dva-1.1',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['step-functions', 'lambda'],
    stem: 'A workflow must pause until a human approves a request, which may take days, then continue. What Step Functions pattern should be used?',
    options: [
      { id: 'A', text: 'A task with .waitForTaskToken, resumed by a call to SendTaskSuccess when the approval arrives.', correct: true, why: 'The callback pattern suspends the execution — for up to a year on a Standard workflow — until something external returns the task token.' },
      { id: 'B', text: 'A Wait state with a long timeout, polling an approvals table.', correct: false, why: 'Wastes state transitions polling, and requires guessing the delay in advance.' },
      { id: 'C', text: 'An Express workflow with a long timeout.', correct: false, why: 'Express workflows are capped at five minutes, so days of waiting is impossible.' },
      { id: 'D', text: 'A Lambda function that sleeps until approval is received.', correct: false, why: 'Lambda caps at 15 minutes, and paying to sleep is exactly what workflow state exists to avoid.' },
    ],
    explanation:
      '.waitForTaskToken is the human-in-the-loop pattern. Standard workflows for anything long or requiring exactly-once; Express for high-volume short executions.',
    source: 'https://docs.aws.amazon.com/step-functions/latest/dg/connect-to-resource.html',
  },
  {
    id: 'dva-d1-012',
    certs: ['DVA-C02'],
    taskId: 'dva-1.3',
    type: 'single',
    difficulty: 3,
    serviceSlugs: ['dynamodb'],
    stem: 'A developer adds a GSI to a busy table. Shortly afterwards, writes to the base table begin to throttle, though the base table\'s own capacity is ample. Why?',
    options: [
      { id: 'A', text: 'The GSI has insufficient write capacity, and a throttled index throttles writes to the base table.', correct: true, why: 'Every base-table write that affects an indexed attribute must also write to the GSI. If the index cannot keep up, the base write fails.' },
      { id: 'B', text: 'GSIs are eventually consistent, so writes queue until the index catches up.', correct: false, why: 'Eventual consistency describes reads of the index, not back-pressure on writes.' },
      { id: 'C', text: 'The GSI must use the same partition key as the base table, and the mismatch causes throttling.', correct: false, why: 'A GSI is specifically allowed a different partition key — that is the whole point of it.' },
      { id: 'D', text: 'Adding a GSI requires a table rebuild, during which writes are throttled.', correct: false, why: 'A GSI backfills online without blocking writes.' },
    ],
    explanation:
      'An under-provisioned GSI breaks the base table. This surprises people and is examined directly — provision indexes as carefully as tables.',
    source: 'https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/GSI.html',
  },

  {
    id: 'dva-d1-013',
    certs: ['DVA-C02'],
    taskId: 'dva-1.2',
    type: 'single',
    difficulty: 3,
    serviceSlugs: ['lambda', 'kinesis-data-streams'],
    stem: 'A Lambda function reads a Kinesis Data Stream. One malformed record throws on every attempt, and processing for that shard has stopped advancing for hours. Records behind it are ageing out. What configuration keeps the shard moving while preserving the bad record for analysis?',
    options: [
      { id: 'A', text: 'On the event source mapping, set BisectBatchOnFunctionError, a MaximumRetryAttempts value, and an on-failure destination.', correct: true, why: 'Bisecting isolates the poison record from the rest of the batch, the retry cap stops it blocking the shard forever, and the destination captures its metadata for investigation.' },
      { id: 'B', text: 'Increase the function timeout so the record has longer to succeed.', correct: false, why: 'The record fails deterministically. More time per attempt only slows the backlog further.' },
      { id: 'C', text: 'Switch the event source to an SQS queue with a dead-letter queue.', correct: false, why: 'It would work for new architecture but discards the per-shard ordering Kinesis was chosen for, and is not a configuration change.' },
      { id: 'D', text: 'Set the starting position to LATEST so processing skips ahead.', correct: false, why: 'StartingPosition only applies when the mapping is created, and skipping ahead silently loses every record in the backlog.' },
    ],
    explanation:
      'A stream shard is ordered, so one failing record blocks everything behind it. The three-part answer — bisect, cap retries, send failures to a destination — is the standard Kinesis and DynamoDB Streams poison-pill fix.',
    source: 'https://docs.aws.amazon.com/lambda/latest/dg/with-kinesis.html',
  },
  {
    id: 'dva-d1-014',
    certs: ['DVA-C02'],
    taskId: 'dva-1.3',
    type: 'single',
    difficulty: 3,
    serviceSlugs: ['dynamodb'],
    stem: 'A table stores IoT readings with partition key deviceId and sort key timestamp. Three of 10,000 devices produce 90% of the traffic and their writes throttle while the table as a whole is well under its provisioned capacity. What change fixes this?',
    options: [
      { id: 'A', text: 'Write-shard the hot key — append a calculated suffix such as deviceId#0..deviceId#9 — and query all shards in parallel on read.', correct: true, why: 'Throughput is allocated per partition, so a single partition key cannot exceed 1,000 WCU regardless of table capacity. Spreading one logical key over several physical keys is the documented remedy.' },
      { id: 'B', text: 'Switch the table to on-demand capacity mode.', correct: false, why: 'On-demand removes the table-level ceiling but the per-partition limit still applies, so the same three devices keep throttling.' },
      { id: 'C', text: 'Add a global secondary index on timestamp.', correct: false, why: 'A GSI adds a read path. It does not relieve write pressure on the base table partition — it adds to it.' },
      { id: 'D', text: 'Enable DynamoDB Accelerator (DAX) in front of the table.', correct: false, why: 'DAX is a read-through cache. Writes still go to the table.' },
    ],
    explanation:
      'Every "capacity is fine but one key throttles" question is about the per-partition limit: 3,000 RCU and 1,000 WCU. The fix is always a better key distribution, never more table capacity.',
    source: 'https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/bp-partition-key-design.html',
  },
  {
    id: 'dva-d1-015',
    certs: ['DVA-C02'],
    taskId: 'dva-1.3',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['elasticache', 'rds'],
    stem: 'An application caches product records in ElastiCache using lazy loading. After a price change is written to the database, users keep seeing the old price for hours. Which change fixes the staleness with the least added write latency?',
    options: [
      { id: 'A', text: 'Keep lazy loading, but set a TTL on each cached item and delete the key when the record is updated.', correct: true, why: 'Invalidating on write bounds staleness to zero for the changed record, and the TTL is the safety net for updates that bypass the application. Neither adds work to the common read path.' },
      { id: 'B', text: 'Switch to write-through caching for every write in the application.', correct: false, why: 'It keeps the cache fresh but adds a cache write to every database write, including records nobody ever reads — the cost the question is asking you to avoid.' },
      { id: 'C', text: 'Reduce the cache node size so entries are evicted sooner.', correct: false, why: 'Relying on memory pressure for correctness is unpredictable, and it evicts hot entries as readily as stale ones.' },
      { id: 'D', text: 'Read from the database and populate the cache asynchronously afterwards.', correct: false, why: 'That is lazy loading described differently. It does not address the stale entry already in the cache.' },
    ],
    explanation:
      'Lazy loading plus a TTL plus explicit invalidation on write is the default caching answer. Write-through is for data that must be readable the instant it is written.',
    source: 'https://docs.aws.amazon.com/AmazonElastiCache/latest/dg/Strategies.html',
  },
  {
    id: 'dva-d1-016',
    certs: ['DVA-C02'],
    taskId: 'dva-1.1',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['api-gateway'],
    stem: 'A new API needs request-body validation against a JSON schema, per-customer API keys with usage plans, and AWS WAF in front of it. Which API Gateway type meets all three requirements?',
    options: [
      { id: 'A', text: 'A REST API.', correct: true, why: 'Request validation with models, API keys with usage plans and WAF integration are all REST API features.' },
      { id: 'B', text: 'An HTTP API.', correct: false, why: 'HTTP APIs are cheaper and lower-latency but have no API keys or usage plans, no request validation and no WAF support.' },
      { id: 'C', text: 'A WebSocket API.', correct: false, why: 'WebSocket APIs are for bidirectional persistent connections, not request/response endpoints.' },
      { id: 'D', text: 'An HTTP API with a Lambda authoriser doing the validation in code.', correct: false, why: 'It could approximate validation but still leaves API keys, usage plans and WAF unmet, and moves platform work into code you have to maintain.' },
    ],
    explanation:
      'Choose HTTP API by default for cost and latency; choose REST API when you need API keys and usage plans, request validation, WAF, or private endpoints.',
    source: 'https://docs.aws.amazon.com/apigateway/latest/developerguide/http-api-vs-rest.html',
  },
  {
    id: 'dva-d1-017',
    certs: ['DVA-C02'],
    taskId: 'dva-1.1',
    type: 'single',
    difficulty: 3,
    serviceSlugs: ['sqs', 'lambda', 'dynamodb'],
    stem: 'A payment worker consumes a standard SQS queue. Occasionally a payment is charged twice. The team confirms the producer sends each message once. What must the worker do?',
    options: [
      { id: 'A', text: 'Make processing idempotent — record the message deduplication id in DynamoDB with a conditional write, and skip the work if the write fails.', correct: true, why: 'Standard queues guarantee at-least-once delivery, so duplicates are expected behaviour and the consumer is responsible for tolerating them. A conditional write is the atomic "have I already done this" check.' },
      { id: 'B', text: 'Increase the visibility timeout to exceed the processing time.', correct: false, why: 'It removes one common cause of duplicates — redelivery mid-processing — but not the at-least-once guarantee itself, so double charges can still occur.' },
      { id: 'C', text: 'Set the queue to long polling with a 20-second wait time.', correct: false, why: 'Long polling reduces empty receives and cost. It has nothing to do with duplicate delivery.' },
      { id: 'D', text: 'Delete the message before processing it rather than after.', correct: false, why: 'That converts a duplicate-processing bug into a lost-payment bug, which is strictly worse.' },
    ],
    explanation:
      'Standard SQS is at-least-once and unordered. FIFO queues offer exactly-once processing within a deduplication window, but the exam expects a consumer to be idempotent either way.',
    source: 'https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/standard-queues.html',
  },
  {
    id: 'dva-d1-018',
    certs: ['DVA-C02'],
    taskId: 'dva-1.1',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['step-functions', 's3', 'lambda'],
    stem: 'A workflow must run the same Lambda transformation over roughly 400,000 objects in an S3 prefix, with concurrency control and per-item error handling. Which Step Functions feature fits?',
    options: [
      { id: 'A', text: 'A Distributed Map state, which reads the S3 inventory directly and runs child executions in large-scale parallelism.', correct: true, why: 'Distributed Map is built for large-scale data processing over S3 and supports item batching, tolerated failure thresholds and a configurable maximum concurrency.' },
      { id: 'B', text: 'An inline Map state over an array of keys passed in the state input.', correct: false, why: 'Inline Map is capped at 40 concurrent iterations and the whole array must fit in the 256 KB state payload — nowhere near 400,000 keys.' },
      { id: 'C', text: 'A Parallel state with one branch per object.', correct: false, why: 'Parallel branches are defined statically in the state machine. You cannot write 400,000 of them.' },
      { id: 'D', text: 'A Choice state looping back on itself once per object.', correct: false, why: 'Sequential iteration with 400,000 state transitions is slow and expensive, and Standard workflows have a 25,000-event execution history limit.' },
    ],
    explanation:
      'Inline Map for small in-payload arrays; Distributed Map when the item source is S3 or the count runs to thousands. The 40-concurrency and 256 KB payload limits are the numbers that decide it.',
    source: 'https://docs.aws.amazon.com/step-functions/latest/dg/concepts-orchestrate-large-scale-parallel-workloads.html',
  },
  {
    id: 'dva-d1-019',
    certs: ['DVA-C02'],
    taskId: 'dva-1.2',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['lambda'],
    stem: 'A Lambda function downloads a 3 GB video from S3, transcodes it on local disk and uploads the result. It fails with "No space left on device". What is the correct fix?',
    options: [
      { id: 'A', text: 'Increase the function ephemeral storage (/tmp) allocation, which can be configured up to 10,240 MB.', correct: true, why: '/tmp defaults to 512 MB and is configurable to 10 GB. It is the exact knob for a function that needs local scratch space.' },
      { id: 'B', text: 'Increase the function memory, since /tmp is carved out of the memory allocation.', correct: false, why: 'Memory and ephemeral storage are separate settings. Raising memory buys CPU, not disk.' },
      { id: 'C', text: 'Write to /var/task instead, which is not size-limited.', correct: false, why: '/var/task holds the deployment package and is read-only at runtime.' },
      { id: 'D', text: 'Enable a Lambda layer to provide additional storage.', correct: false, why: 'Layers add read-only code and dependencies, counting against the 250 MB unzipped package limit.' },
    ],
    explanation:
      'Three separate Lambda limits worth knowing cold: 512 MB default /tmp configurable to 10 GB, 250 MB unzipped package including layers, and 10 GB for a container image. Mount EFS when the working set exceeds even 10 GB or must be shared.',
    source: 'https://docs.aws.amazon.com/lambda/latest/dg/configuration-ephemeral-storage.html',
  },
  {
    id: 'dva-d1-020',
    certs: ['DVA-C02'],
    taskId: 'dva-1.1',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['s3', 'api-gateway', 'lambda'],
    stem: 'Browser clients must upload files of up to 200 MB straight to S3 without the bytes passing through the application, and the upload link must expire after 15 minutes. What should the backend return to the client?',
    options: [
      { id: 'A', text: 'A presigned URL generated with the caller credentials of a role that can write only to the intended prefix.', correct: true, why: 'A presigned URL carries the signer permissions and an expiry, so the browser uploads directly to S3 and the application never handles the payload.' },
      { id: 'B', text: 'Temporary IAM user access keys created per upload.', correct: false, why: 'Creating IAM users per request is an anti-pattern — it is rate-limited, leaves credentials to clean up and is far broader than one upload needs.' },
      { id: 'C', text: 'A public-write bucket policy scoped to the upload prefix.', correct: false, why: 'Anonymous write access to a prefix lets anyone on the internet fill the bucket.' },
      { id: 'D', text: 'A base64 upload through API Gateway to a Lambda function that forwards it to S3.', correct: false, why: 'API Gateway caps the payload at 10 MB, and the requirement is explicitly that bytes should not pass through the application.' },
    ],
    explanation:
      'Presigned URL for direct client transfer, expiring and scoped. Remember the API Gateway 10 MB payload limit and the Lambda 6 MB synchronous response limit — they are why the direct-to-S3 pattern exists.',
    source: 'https://docs.aws.amazon.com/AmazonS3/latest/userguide/PresignedUrlUploadObject.html',
  },
  {
    id: 'dva-d1-021',
    certs: ['DVA-C02'],
    taskId: 'dva-1.1',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['eventbridge', 'sns'],
    stem: 'An order service must publish events that several teams consume. Consumers need to subscribe to events whose JSON body matches specific field values, and one team needs to replay last month\'s events into a new service. Which service should carry the events?',
    options: [
      { id: 'A', text: 'Amazon EventBridge, using rules with content-based event patterns and an event archive with replay.', correct: true, why: 'Event patterns match on the event payload structure, and archive plus replay is an EventBridge feature. Both requirements point to the same service.' },
      { id: 'B', text: 'Amazon SNS with subscription filter policies.', correct: false, why: 'SNS filter policies match message attributes rather than the message body by default, and SNS has no archive and replay capability.' },
      { id: 'C', text: 'Amazon SQS with one queue per consumer.', correct: false, why: 'A queue is point-to-point. Fanning out to several teams needs a topic or bus in front, and there is no content routing or replay.' },
      { id: 'D', text: 'Kinesis Data Streams with a 30-day retention setting.', correct: false, why: 'Retention would allow re-reading, but consumers must filter every record themselves and manage shard-level consumption — far heavier than rule-based routing.' },
    ],
    explanation:
      'SNS for high-throughput fanout of simple notifications; EventBridge when you need content-based routing, schema discovery, replay, or third-party and AWS service events.',
    source: 'https://docs.aws.amazon.com/eventbridge/latest/userguide/eb-archive.html',
  },
  /* ── Domain 2: Security (26%) ─────────────────────────────────────────── */
  {
    id: 'dva-d2-001',
    certs: ['DVA-C02'],
    taskId: 'dva-2.1',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['cognito'],
    stem: 'A mobile app must let users sign in with email and password, then upload files directly to S3 using scoped AWS credentials. Which Cognito components are needed?',
    options: [
      { id: 'A', text: 'A user pool for authentication and an identity pool to exchange the resulting token for temporary AWS credentials.', correct: true, why: 'The user pool is the directory that authenticates; the identity pool calls STS to convert that token into scoped AWS credentials the client can use against S3.' },
      { id: 'B', text: 'A user pool only — its tokens can be used directly as AWS credentials.', correct: false, why: 'A user pool issues JWTs for your application, not SigV4 credentials for AWS APIs.' },
      { id: 'C', text: 'An identity pool only, with users created inside it.', correct: false, why: 'Identity pools do not store users or handle sign-in; they federate an existing identity.' },
      { id: 'D', text: 'A user pool plus an IAM user per application user.', correct: false, why: 'IAM users are for AWS principals, never for application customers.' },
    ],
    explanation:
      'User pool = who you are. Identity pool = what AWS credentials you get. Questions needing both sign-in and direct AWS access need both.',
    source: 'https://docs.aws.amazon.com/cognito/latest/developerguide/what-is-amazon-cognito.html',
  },
  {
    id: 'dva-d2-002',
    certs: ['DVA-C02'],
    taskId: 'dva-2.2',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['kms'],
    stem: 'An application must encrypt 50 MB files before storing them in S3, using a key managed in KMS. What is the correct approach?',
    options: [
      { id: 'A', text: 'Envelope encryption: call GenerateDataKey, encrypt the file locally with the plaintext data key, and store the encrypted data key alongside the ciphertext.', correct: true, why: 'KMS can only encrypt up to 4 KB directly. Envelope encryption is the designed pattern, and KMS never sees the bulk data.' },
      { id: 'B', text: 'Call kms:Encrypt with the file contents as the plaintext parameter.', correct: false, why: 'The Encrypt API rejects payloads over 4 KB, so a 50 MB file cannot be passed to it.' },
      { id: 'C', text: 'Split the file into 4 KB chunks and call kms:Encrypt on each.', correct: false, why: 'Technically possible and completely impractical — 12,800 API calls per file, and reassembly complexity for no benefit.' },
      { id: 'D', text: 'Store the KMS key material in the application and encrypt locally.', correct: false, why: 'KMS key material never leaves KMS. That is the security property the service exists to provide.' },
    ],
    explanation:
      'The 4 KB limit is why envelope encryption exists. SSE-KMS on S3 does this for you; client-side encryption means doing it explicitly.',
    source: 'https://docs.aws.amazon.com/kms/latest/developerguide/concepts.html',
  },
  {
    id: 'dva-d2-003',
    certs: ['DVA-C02'],
    taskId: 'dva-2.3',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['secrets-manager', 'lambda'],
    stem: 'A Lambda function currently reads a database password from a plaintext environment variable. Security requires that the credential be encrypted at rest, rotated automatically, and not visible to anyone who can view the function configuration. What should be used?',
    options: [
      { id: 'A', text: 'Store the credential in AWS Secrets Manager with rotation enabled, and retrieve it at run time using the execution role.', correct: true, why: 'It is encrypted with KMS, rotated on a schedule, and never appears in the function configuration where anyone with lambda:GetFunction could read it.' },
      { id: 'B', text: 'Encrypt the environment variable with a KMS key and decrypt it in the handler.', correct: false, why: 'Better than plaintext, but there is still no rotation, and the ciphertext remains in the function configuration.' },
      { id: 'C', text: 'Store it in an SSM Parameter Store String parameter.', correct: false, why: 'A String parameter is not encrypted at all. SecureString would encrypt but still would not rotate.' },
      { id: 'D', text: 'Pass the credential in the event payload from the caller.', correct: false, why: 'It moves the secret into logs and traces, which is considerably worse.' },
    ],
    explanation:
      'Rotation is the discriminator. Cache the retrieved secret outside the handler so you are not calling the API on every invocation.',
    source: 'https://docs.aws.amazon.com/secretsmanager/latest/userguide/intro.html',
  },
  {
    id: 'dva-d2-004',
    certs: ['DVA-C02'],
    taskId: 'dva-2.1',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['api-gateway', 'cognito', 'lambda'],
    stem: 'An API must be authorised using a third-party OIDC provider\'s JWTs, with custom logic that checks a claim against a DynamoDB allow-list. Which API Gateway authoriser fits?',
    options: [
      { id: 'A', text: 'A Lambda authoriser, which can validate the token and run arbitrary authorisation logic.', correct: true, why: 'Custom logic beyond token validation — such as a database lookup — is precisely what a Lambda authoriser is for, and its result can be cached.' },
      { id: 'B', text: 'A Cognito user pool authoriser.', correct: false, why: 'That validates tokens issued by a Cognito user pool, not a third-party provider, and cannot run custom logic.' },
      { id: 'C', text: 'IAM authorisation with SigV4 signing.', correct: false, why: 'That authenticates AWS principals, not application users holding OIDC tokens.' },
      { id: 'D', text: 'A resource policy on the API restricting source IPs.', correct: false, why: 'Network-level restriction, unrelated to token-based authorisation.' },
    ],
    explanation:
      'Cognito token → Cognito authoriser. Third-party token or custom logic → Lambda authoriser. AWS principal → IAM.',
    source: 'https://docs.aws.amazon.com/apigateway/latest/developerguide/apigateway-use-lambda-authorizer.html',
  },
  {
    id: 'dva-d2-005',
    certs: ['DVA-C02'],
    taskId: 'dva-2.2',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['kms', 's3'],
    stem: 'Account A must let a role in Account B decrypt objects in an S3 bucket encrypted with a customer managed KMS key in Account A. What must be configured?',
    options: [
      { id: 'A', text: 'The bucket policy must allow the Account B role, the KMS key policy must allow it kms:Decrypt, and Account B\'s identity policy must allow both actions.', correct: true, why: 'Three policies must all agree: the resource policy, the key policy, and the caller\'s identity policy. Missing the key policy is the usual failure.' },
      { id: 'B', text: 'Only the bucket policy needs to allow the Account B role.', correct: false, why: 'The object cannot be decrypted without the key policy also permitting the role.' },
      { id: 'C', text: 'Only Account B\'s identity policy needs s3:GetObject and kms:Decrypt.', correct: false, why: 'Cross-account access needs the resource side to allow it too — an identity policy alone cannot reach into another account.' },
      { id: 'D', text: 'The key must be converted to an AWS managed key so it can be shared.', correct: false, why: 'AWS managed keys cannot be shared cross-account at all. Only customer managed keys can.' },
    ],
    explanation:
      'Cross-account plus SSE-KMS means three policies. The key policy is authoritative and is the one people forget.',
    source: 'https://docs.aws.amazon.com/kms/latest/developerguide/key-policy-modifying-external-accounts.html',
  },
  {
    id: 'dva-d2-006',
    certs: ['DVA-C02'],
    taskId: 'dva-2.1',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['sts', 'iam'],
    stem: 'An application running on ECS Fargate must call an API in another AWS account. What is the correct approach?',
    options: [
      { id: 'A', text: 'Have the task role call sts:AssumeRole on a role in the target account whose trust policy allows the task role.', correct: true, why: 'Role assumption yields temporary credentials scoped to the target account, with both sides explicitly configured.' },
      { id: 'B', text: 'Store an IAM user\'s access keys for the target account in the task definition environment variables.', correct: false, why: 'Long-lived credentials visible to anyone who can describe the task definition.' },
      { id: 'C', text: 'Add the target account to the task execution role\'s policy.', correct: false, why: 'The execution role pulls images and writes logs; it is not the application\'s identity, and cross-account needs the target side too.' },
      { id: 'D', text: 'Enable cross-account access in the ECS cluster settings.', correct: false, why: 'No such setting exists.' },
    ],
    explanation:
      'Also worth knowing the ECS pair: the *execution* role pulls the image and writes logs; the *task* role is what your code uses.',
    source: 'https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task-iam-roles.html',
  },

  {
    id: 'dva-d2-007',
    certs: ['DVA-C02'],
    taskId: 'dva-2.1',
    type: 'single',
    difficulty: 3,
    serviceSlugs: ['iam', 's3', 'lambda'],
    stem: 'A Lambda execution role grants s3:GetObject on arn:aws:s3:::reports/*. The bucket policy contains an explicit Deny for that action unless aws:SourceVpce matches a specific VPC endpoint. The function is not in a VPC. What happens?',
    options: [
      { id: 'A', text: 'The call is denied, because an explicit Deny in any applicable policy overrides every Allow.', correct: true, why: 'Evaluation order is: explicit Deny wins, then an Allow is required from somewhere. The condition is unmet, so the Deny applies and nothing can override it.' },
      { id: 'B', text: 'The call succeeds, because the identity policy grants the action.', correct: false, why: 'For same-account access an Allow in either policy is sufficient — but only in the absence of an explicit Deny, which this bucket policy supplies.' },
      { id: 'C', text: 'The call succeeds, because a resource policy cannot restrict a service role.', correct: false, why: 'Bucket policies apply to every principal, including service-assumed roles.' },
      { id: 'D', text: 'The call is denied only if the role also lacks s3:ListBucket.', correct: false, why: 'ListBucket governs listing keys. It is irrelevant to a GetObject decision.' },
    ],
    explanation:
      'Memorise the order: explicit Deny, then SCP boundary, then permissions boundary, then session policy, then Allow. An exam question that mentions a Deny anywhere has already told you the answer.',
    source: 'https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_policies_evaluation-logic.html',
  },
  {
    id: 'dva-d2-008',
    certs: ['DVA-C02'],
    taskId: 'dva-2.1',
    type: 'single',
    difficulty: 3,
    serviceSlugs: ['cognito'],
    stem: 'A single-page web application signs users in through the Cognito hosted UI. Security review rejects the current implicit-grant configuration because the access token appears in the browser URL. Which flow should replace it?',
    options: [
      { id: 'A', text: 'Authorization code grant with PKCE, exchanging the code for tokens from the app.', correct: true, why: 'The code grant keeps tokens out of the redirect URL, and PKCE lets a public client complete the exchange safely without a client secret.' },
      { id: 'B', text: 'Authorization code grant with a client secret embedded in the JavaScript bundle.', correct: false, why: 'A secret shipped to the browser is not a secret. Public clients must use PKCE instead.' },
      { id: 'C', text: 'Client credentials grant.', correct: false, why: 'That flow is for machine-to-machine access with no user, so it cannot authenticate a person.' },
      { id: 'D', text: 'Resource owner password grant, collecting credentials in the app\'s own form.', correct: false, why: 'It puts the user password through your application and loses hosted-UI features such as MFA and federation.' },
    ],
    explanation:
      'Public clients — browsers and mobile apps — use the authorization code grant with PKCE. Confidential server-side clients may use a client secret. Implicit grant is deprecated.',
    source: 'https://docs.aws.amazon.com/cognito/latest/developerguide/federation-endpoints-oauth-grants.html',
  },
  {
    id: 'dva-d2-009',
    certs: ['DVA-C02'],
    taskId: 'dva-2.1',
    type: 'multi',
    difficulty: 3,
    serviceSlugs: ['ecs', 'ecr', 'iam'],
    stem: 'An ECS task on Fargate must pull its image from a private ECR repository and, once running, write to a DynamoDB table. Which two role assignments are required? (Choose two.)',
    options: [
      { id: 'A', text: 'A task execution role with permission to pull from ECR and write task logs.', correct: true, why: 'The execution role is used by the ECS agent — the infrastructure — to pull images and create log streams before the container starts.' },
      { id: 'B', text: 'A task role with permission to write to the DynamoDB table.', correct: true, why: 'The task role is what the application code inside the container assumes, so it carries the permissions the code needs at runtime.' },
      { id: 'C', text: 'An EC2 instance profile with both sets of permissions.', correct: false, why: 'Fargate has no instance you own, so there is no instance profile to attach.' },
      { id: 'D', text: 'Access keys for an IAM user, injected as container environment variables.', correct: false, why: 'Long-lived keys in environment variables are exactly what task roles exist to eliminate.' },
      { id: 'E', text: 'A service-linked role for Amazon ECS granting DynamoDB access.', correct: false, why: 'The service-linked role lets ECS manage load balancers and network interfaces on your behalf. It is not a place to grant application permissions.' },
    ],
    explanation:
      'Two roles, two audiences: execution role for the platform (pull image, ship logs, read secrets for injection), task role for your code. Mixing them up is a standard DVA distractor.',
    source: 'https://docs.aws.amazon.com/AmazonECS/latest/developerguide/task-iam-roles.html',
  },
  {
    id: 'dva-d2-010',
    certs: ['DVA-C02'],
    taskId: 'dva-2.2',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['s3', 'kms', 'cloudtrail'],
    stem: 'A compliance rule requires that every decryption of objects in an S3 bucket be attributable to a principal in an audit log, and that the key be rotatable on the team\'s own schedule. Which server-side encryption option meets both requirements?',
    options: [
      { id: 'A', text: 'SSE-KMS with a customer managed key.', correct: true, why: 'Every Decrypt call against the key is a CloudTrail event naming the principal, and a customer managed key can be rotated on demand or annually.' },
      { id: 'B', text: 'SSE-S3 with the Amazon S3 managed key.', correct: false, why: 'It encrypts at rest with no configuration, but there is no per-request audit trail and no key you can rotate.' },
      { id: 'C', text: 'SSE-KMS with an AWS managed key (aws/s3).', correct: false, why: 'It produces CloudTrail events but the rotation schedule is fixed by AWS and the key policy cannot be edited.' },
      { id: 'D', text: 'SSE-C, with the application supplying the key on every request.', correct: false, why: 'You keep full control but also full responsibility for storage and rotation, and KMS logs nothing because KMS is not involved.' },
    ],
    explanation:
      '"Who decrypted what, and when" plus "we control rotation" means a customer managed KMS key. Consider S3 Bucket Keys alongside it to cut KMS request costs on high-volume buckets.',
    source: 'https://docs.aws.amazon.com/AmazonS3/latest/userguide/UsingKMSEncryption.html',
  },
  {
    id: 'dva-d2-011',
    certs: ['DVA-C02'],
    taskId: 'dva-2.2',
    type: 'single',
    difficulty: 3,
    serviceSlugs: ['kms', 'lambda'],
    stem: 'A batch job running under a role in another account needs to decrypt with a customer managed KMS key for the duration of one nightly run, and the key policy must not be edited for every such job. What should be used?',
    options: [
      { id: 'A', text: 'A KMS grant scoped to the grantee principal and the kms:Decrypt operation, retired when the job finishes.', correct: true, why: 'Grants are the programmatic, temporary alternative to editing a key policy, and they can be retired or revoked when no longer needed.' },
      { id: 'B', text: 'A new key policy statement naming the external role, removed the next morning.', correct: false, why: 'It works but is exactly the churn the question rules out, and a forgotten statement is a standing grant of decrypt.' },
      { id: 'C', text: 'A key alias pointing at the key, shared with the other account.', correct: false, why: 'An alias is a friendly name. It conveys no permission at all.' },
      { id: 'D', text: 'Enabling automatic key rotation.', correct: false, why: 'Rotation changes the backing key material. It is unrelated to who may use the key.' },
    ],
    explanation:
      'Key policy for durable access, grants for temporary and programmatic access. Grants also underpin how AWS services obtain permission for encryption on your behalf.',
    source: 'https://docs.aws.amazon.com/kms/latest/developerguide/grants.html',
  },
  {
    id: 'dva-d2-012',
    certs: ['DVA-C02'],
    taskId: 'dva-2.2',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['rds', 'acm'],
    stem: 'An auditor finds that some application instances connect to Amazon RDS for PostgreSQL without TLS. The team wants connections without TLS to be impossible, not merely discouraged. What should be done?',
    options: [
      { id: 'A', text: 'Set rds.force_ssl to 1 in the DB parameter group and have clients trust the Amazon RDS certificate authority bundle.', correct: true, why: 'Forcing SSL at the engine level makes the server reject plaintext connections, so client misconfiguration fails loudly instead of silently downgrading.' },
      { id: 'B', text: 'Enable encryption at rest with KMS on the instance.', correct: false, why: 'At-rest encryption protects stored data. It has no effect on the wire protocol.' },
      { id: 'C', text: 'Move the database into a private subnet with a restrictive security group.', correct: false, why: 'Network isolation limits who can connect, not whether the connection is encrypted.' },
      { id: 'D', text: 'Issue a certificate from AWS Certificate Manager and install it on the database.', correct: false, why: 'RDS supplies and rotates its own server certificate; you cannot install an ACM public certificate on it.' },
    ],
    explanation:
      'In transit and at rest are separate controls with separate switches. For RDS, the in-transit control is the engine parameter — rds.force_ssl for PostgreSQL, require_secure_transport for MySQL.',
    source: 'https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/PostgreSQL.Concepts.General.SSL.html',
  },
  {
    id: 'dva-d2-013',
    certs: ['DVA-C02'],
    taskId: 'dva-2.3',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['systems-manager', 'secrets-manager'],
    stem: 'A team must store about 4,000 non-secret configuration values — feature flags, endpoint URLs, timeouts — with version history and hierarchical paths, at the lowest possible cost. What should they use?',
    options: [
      { id: 'A', text: 'Systems Manager Parameter Store standard parameters.', correct: true, why: 'Standard parameters are free, versioned, support hierarchical paths and GetParametersByPath, and allow 10,000 per account per Region — comfortably covering 4,000 values.' },
      { id: 'B', text: 'AWS Secrets Manager, one secret per value.', correct: false, why: 'It would work, but it charges per secret per month plus API calls for data that is not secret and needs no rotation.' },
      { id: 'C', text: 'Lambda environment variables on each consuming function.', correct: false, why: 'Configuration is then duplicated per function, requires a deployment to change, and hits the 4 KB total environment variable limit.' },
      { id: 'D', text: 'A DynamoDB table read at start-up.', correct: false, why: 'It is buildable but you are re-implementing versioning, hierarchy and access control that Parameter Store already provides.' },
    ],
    explanation:
      'Parameter Store for configuration, including secrets you rotate yourself with SecureString. Secrets Manager when you want managed rotation, cross-account resource policies, or generated passwords — and you accept the per-secret charge.',
    source: 'https://docs.aws.amazon.com/systems-manager/latest/userguide/systems-manager-parameter-store.html',
  },
  {
    id: 'dva-d2-014',
    certs: ['DVA-C02'],
    taskId: 'dva-2.3',
    type: 'single',
    difficulty: 1,
    serviceSlugs: ['sdk', 'iam', 'ec2'],
    stem: 'An application on EC2 currently loads access keys from a config file committed to the repository. What is the correct replacement?',
    options: [
      { id: 'A', text: 'Attach an instance profile with the needed permissions and let the SDK default credential provider chain retrieve temporary credentials from the instance metadata service.', correct: true, why: 'Role-based temporary credentials are rotated automatically and never appear in source, and the SDK finds them with no code change.' },
      { id: 'B', text: 'Move the keys into an encrypted environment variable on the instance.', correct: false, why: 'It hides the keys from the repository but they remain long-lived credentials that someone must rotate and that leak with any process dump.' },
      { id: 'C', text: 'Store the keys in Secrets Manager and fetch them at start-up.', correct: false, why: 'Better than a config file, but a role removes the credential entirely rather than relocating it — and fetching the secret itself needs credentials.' },
      { id: 'D', text: 'Add the config file to .gitignore and rotate the keys quarterly.', correct: false, why: 'It reduces exposure without changing the fact that a static key exists to be stolen.' },
    ],
    explanation:
      'The credential provider chain order — environment, shared config, container credentials, then IMDS — is worth knowing. The principle behind it is the exam answer: roles, never keys.',
    source: 'https://docs.aws.amazon.com/sdkref/latest/guide/standardized-credentials.html',
  },
  {
    id: 'dva-d2-015',
    certs: ['DVA-C02'],
    taskId: 'dva-2.1',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['api-gateway', 'iam'],
    stem: 'An internal REST API must be callable only by two specific IAM roles in the same organisation, with no user-facing sign-in. Which combination is correct?',
    options: [
      { id: 'A', text: 'IAM authorisation on the method, callers signing requests with SigV4, plus a resource policy restricting the allowed principals.', correct: true, why: 'IAM auth validates the signature and identity, and the resource policy pins access to the two roles — defence in depth with no user pool involved.' },
      { id: 'B', text: 'An API key per calling service, distributed with the client configuration.', correct: false, why: 'API keys identify a caller for usage plans and throttling. AWS documentation is explicit that they are not an authentication mechanism.' },
      { id: 'C', text: 'A Cognito user pool authoriser with a machine user in the pool.', correct: false, why: 'It introduces a user directory for callers that already have IAM identities.' },
      { id: 'D', text: 'A Lambda authoriser that checks a shared bearer token from Secrets Manager.', correct: false, why: 'It works but re-implements authentication, adds latency and creates a shared secret where SigV4 needs none.' },
    ],
    explanation:
      'Service-to-service inside AWS means SigV4 with IAM authorisation. API keys are for metering, not identity — that distinction is tested directly.',
    source: 'https://docs.aws.amazon.com/apigateway/latest/developerguide/apigateway-control-access-to-api.html',
  },
  {
    id: 'dva-d2-016',
    certs: ['DVA-C02'],
    taskId: 'dva-2.2',
    type: 'single',
    difficulty: 3,
    serviceSlugs: ['dynamodb', 'kms'],
    stem: 'A table stores customer records. Regulation requires that a national identifier field be unreadable to anyone with only DynamoDB read permission, including database administrators, while other attributes stay queryable. What should be implemented?',
    options: [
      { id: 'A', text: 'Client-side encryption of that attribute with the AWS Database Encryption SDK for DynamoDB, so the ciphertext is what the table stores.', correct: true, why: 'Encrypting in the application means a reader with only table access sees ciphertext — separating data access from key access, which is the requirement.' },
      { id: 'B', text: 'Rely on DynamoDB encryption at rest with a customer managed key.', correct: false, why: 'At-rest encryption is transparent: any principal allowed to read the table gets plaintext back. It protects the storage medium, not the reader.' },
      { id: 'C', text: 'Move the field to a separate table with a restrictive resource policy.', correct: false, why: 'It narrows who can read the field, but the value is still stored and returned in plaintext to whoever can read that table.' },
      { id: 'D', text: 'Use a fine-grained access policy with dynamodb:Attributes to hide the field.', correct: false, why: 'That condition key can restrict which attributes a request may touch, but it does not protect the value from a principal whose policy permits it.' },
    ],
    explanation:
      'At-rest encryption defends against someone stealing the disk. Client-side encryption defends against someone with legitimate database access. When a question says "including administrators", it is asking for client-side.',
    source: 'https://docs.aws.amazon.com/database-encryption-sdk/latest/devguide/what-is-database-encryption-sdk.html',
  },
  {
    id: 'dva-d2-017',
    certs: ['DVA-C02'],
    taskId: 'dva-2.1',
    type: 'single',
    difficulty: 3,
    serviceSlugs: ['cognito', 'api-gateway'],
    stem: 'An API authorises requests from the tenant_id claim in a Cognito ID token. The value lives in a DynamoDB profile table, not in the user pool. The team wants it present in every issued token without changing the API. What should be configured?',
    options: [
      { id: 'A', text: 'A pre token generation Lambda trigger on the user pool that adds tenant_id as a custom claim.', correct: true, why: 'That trigger runs during token issuance and exists precisely to add, modify or suppress claims, so every token carries the value.' },
      { id: 'B', text: 'A post authentication Lambda trigger that writes tenant_id back to the user pool.', correct: false, why: 'It fires after the token has already been issued, so the current token still lacks the claim.' },
      { id: 'C', text: 'A custom attribute on the user pool, populated at sign-up.', correct: false, why: 'It would appear in the token but duplicates data that lives in DynamoDB and goes stale when tenancy changes.' },
      { id: 'D', text: 'A Lambda authoriser on API Gateway that looks up tenant_id per request.', correct: false, why: 'It resolves the value but adds a lookup to every call and changes the API configuration, which the question rules out.' },
    ],
    explanation:
      'Know the Cognito triggers by when they fire: pre sign-up, pre authentication, post authentication, pre token generation, migrate user, custom message. Claim shaping is always pre token generation.',
    source: 'https://docs.aws.amazon.com/cognito/latest/developerguide/user-pool-lambda-pre-token-generation.html',
  },
  /* ── Domain 3: Deployment (24%) ───────────────────────────────────────── */
  {
    id: 'dva-d3-001',
    certs: ['DVA-C02'],
    taskId: 'dva-3.4',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['codedeploy', 'lambda'],
    stem: 'A new Lambda version must receive 10% of traffic for five minutes, then all of it, rolling back automatically if the error rate rises. What should be configured?',
    options: [
      { id: 'A', text: 'A CodeDeploy deployment with the Canary10Percent5Minutes configuration and a CloudWatch alarm as the rollback trigger.', correct: true, why: 'That deployment configuration is literally 10% for five minutes then the remainder, and an alarm on the alias triggers automatic rollback.' },
      { id: 'B', text: 'A weighted alias split 10/90, adjusted manually after five minutes.', correct: false, why: 'It achieves the split but requires a human to complete the shift and to notice the errors.' },
      { id: 'C', text: 'The Linear10PercentEvery1Minute configuration.', correct: false, why: 'That shifts 10% every minute over ten minutes, which is not the pattern described.' },
      { id: 'D', text: 'A blue/green deployment on the function\'s $LATEST version.', correct: false, why: '$LATEST cannot be traffic-weighted; canary deployments require a published version behind an alias.' },
    ],
    explanation:
      'Canary means two jumps; linear means equal increments. Naming the specific configuration is a DVA habit worth having.',
    source: 'https://docs.aws.amazon.com/codedeploy/latest/userguide/deployment-configurations.html',
  },
  {
    id: 'dva-d3-002',
    certs: ['DVA-C02'],
    taskId: 'dva-3.4',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['api-gateway', 'lambda'],
    stem: 'One API Gateway deployment must serve dev, test and prod, each invoking a different Lambda alias, without duplicating the API definition. What should be used?',
    options: [
      { id: 'A', text: 'Stage variables referencing the Lambda alias, with one stage per environment.', correct: true, why: 'A stage variable substituted into the integration URI lets a single API definition target a different alias per stage.' },
      { id: 'B', text: 'Three separate REST APIs, one per environment.', correct: false, why: 'Duplicates the definition, which the requirement excludes.' },
      { id: 'C', text: 'A Lambda authoriser that routes to the correct alias.', correct: false, why: 'An authoriser authorises; it does not choose integration targets.' },
      { id: 'D', text: 'Custom domain base-path mappings pointing at different functions.', correct: false, why: 'Base-path mappings route to stages, not to different integration targets within one stage.' },
    ],
    explanation:
      'Stage variables plus Lambda aliases is the DVA answer to environment-specific integrations from one API definition.',
    source: 'https://docs.aws.amazon.com/apigateway/latest/developerguide/stage-variables.html',
  },
  {
    id: 'dva-d3-003',
    certs: ['DVA-C02'],
    taskId: 'dva-3.2',
    type: 'single',
    difficulty: 1,
    serviceSlugs: ['sam'],
    stem: 'A developer wants to invoke a Lambda function locally with a test event, and run the API locally before deploying. What should be used?',
    options: [
      { id: 'A', text: 'AWS SAM CLI, using sam local invoke and sam local start-api.', correct: true, why: 'SAM CLI runs functions in a Docker container matching the Lambda runtime and can emulate API Gateway locally.' },
      { id: 'B', text: 'The AWS CLI with aws lambda invoke.', correct: false, why: 'That invokes the deployed function in AWS, not a local one.' },
      { id: 'C', text: 'AWS CloudShell with the function code uploaded.', correct: false, why: 'CloudShell is a remote shell, not a local Lambda emulator.' },
      { id: 'D', text: 'AWS CodeBuild with a buildspec that runs the handler.', correct: false, why: 'CodeBuild runs in AWS and is for building, not for local iteration.' },
    ],
    explanation:
      'SAM CLI is the local development loop: sam local invoke, sam local start-api, sam local generate-event. Docker is a prerequisite.',
    source: 'https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/serverless-sam-cli-using-invoke.html',
  },
  {
    id: 'dva-d3-004',
    certs: ['DVA-C02'],
    taskId: 'dva-3.1',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['appconfig'],
    stem: 'A team wants to enable a new feature for 10% of users, then all of them, rolling back automatically if the error metric rises — without deploying new code. What should be used?',
    options: [
      { id: 'A', text: 'AWS AppConfig feature flags with a gradual deployment strategy and a CloudWatch alarm for automatic rollback.', correct: true, why: 'AppConfig deploys configuration progressively, validates it, monitors an alarm, and rolls the configuration back on its own — all without a code deployment.' },
      { id: 'B', text: 'An SSM Parameter Store parameter read at start-up.', correct: false, why: 'It stores the flag but has no gradual rollout, no validation and no automatic rollback.' },
      { id: 'C', text: 'Lambda environment variables updated by a pipeline.', correct: false, why: 'Changing them is a function update — a deployment — which the requirement excludes.' },
      { id: 'D', text: 'A CodeDeploy canary deployment of the function.', correct: false, why: 'That deploys code; the requirement is to change behaviour without deploying.' },
    ],
    explanation:
      'Feature flags with gradual rollout and alarm-triggered rollback is the AppConfig signature. Parameter Store is the plausible distractor.',
    source: 'https://docs.aws.amazon.com/appconfig/latest/userguide/what-is-appconfig.html',
  },
  {
    id: 'dva-d3-005',
    certs: ['DVA-C02'],
    taskId: 'dva-3.1',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['lambda', 'ecr'],
    stem: 'A Lambda function needs a 600 MB machine-learning dependency. What packaging option supports this?',
    options: [
      { id: 'A', text: 'A container image in Amazon ECR, which supports images up to 10 GB.', correct: true, why: 'The .zip path caps at 250 MB unzipped. Container image packaging raises the ceiling to 10 GB.' },
      { id: 'B', text: 'A Lambda layer, which has its own separate size allowance.', correct: false, why: 'Layers count toward the same 250 MB unzipped limit, so they do not help here.' },
      { id: 'C', text: 'A .zip uploaded via S3 rather than directly.', correct: false, why: 'Uploading via S3 raises the *upload* limit but the 250 MB unzipped limit still applies.' },
      { id: 'D', text: 'Mount the dependency from EFS and keep the package small.', correct: false, why: 'Technically viable and a real pattern, but it adds VPC configuration and cold-start cost — container packaging is the direct answer.' },
    ],
    explanation:
      'The numbers matter: 50 MB zipped direct upload, 250 MB unzipped including layers, 10 GB as a container image.',
    source: 'https://docs.aws.amazon.com/lambda/latest/dg/gettingstarted-limits.html',
  },
  {
    id: 'dva-d3-006',
    certs: ['DVA-C02'],
    taskId: 'dva-3.4',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['codebuild', 'secrets-manager'],
    stem: 'A CodeBuild project needs a third-party API token during the build. It is currently a plaintext environment variable in the buildspec, visible in the console. What should be done?',
    options: [
      { id: 'A', text: 'Store the token in Secrets Manager and reference it with the SECRETS_MANAGER environment variable type.', correct: true, why: 'CodeBuild resolves the reference at build time, so the value never appears in the buildspec, the project configuration or the console.' },
      { id: 'B', text: 'Base64-encode the token in the buildspec.', correct: false, why: 'Encoding is not encryption — anyone reading the buildspec can decode it trivially.' },
      { id: 'C', text: 'Pass the token as a build override on every manual start.', correct: false, why: 'It breaks automated pipeline builds and puts the secret in command history.' },
      { id: 'D', text: 'Store it in the source repository in an encrypted file and decrypt during the build.', correct: false, why: 'Secrets in source control, plus a decryption key that must also be available. Worse on both counts.' },
    ],
    explanation:
      'CodeBuild supports PARAMETER_STORE and SECRETS_MANAGER variable types precisely so secrets stay out of the buildspec.',
    source: 'https://docs.aws.amazon.com/codebuild/latest/userguide/build-spec-ref.html',
  },

  {
    id: 'dva-d3-007',
    certs: ['DVA-C02'],
    taskId: 'dva-3.3',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['codepipeline', 'codebuild'],
    stem: 'A pipeline builds, then deploys to staging, then deploys to production. Integration tests must run against staging and production deployment must not start if they fail. How should the pipeline be structured?',
    options: [
      { id: 'A', text: 'Add a test stage after the staging deploy containing a CodeBuild test action — a failed action stops the pipeline before the production stage.', correct: true, why: 'CodePipeline promotes an artefact to the next stage only when every action in the current stage succeeds, so a failing test action is itself the gate.' },
      { id: 'B', text: 'Run the integration tests inside the build stage before deploying anywhere.', correct: false, why: 'At build time there is no deployed staging environment to test against.' },
      { id: 'C', text: 'Add a manual approval action and have a person run the tests by hand.', correct: false, why: 'The requirement is automated deployment testing, and a human gate reintroduces the delay and inconsistency automation removes.' },
      { id: 'D', text: 'Configure a CloudWatch alarm on staging errors and a pipeline rule that watches it.', correct: false, why: 'An alarm reports symptoms after the fact and does not deterministically block promotion of this execution.' },
    ],
    explanation:
      'In CodePipeline, ordering is the control flow: stages run in sequence, actions within a stage can run in parallel by run order, and any failed action halts that execution.',
    source: 'https://docs.aws.amazon.com/codepipeline/latest/userguide/reference-pipeline-structure.html',
  },
  {
    id: 'dva-d3-008',
    certs: ['DVA-C02'],
    taskId: 'dva-3.4',
    type: 'single',
    difficulty: 3,
    serviceSlugs: ['codedeploy', 'ec2'],
    stem: 'A CodeDeploy in-place deployment to EC2 must run a smoke test against the new revision and fail the deployment before any user traffic reaches it. Which appspec.yml lifecycle hook should run the test?',
    options: [
      { id: 'A', text: 'ValidateService, which runs after the application starts and can fail the deployment for that instance.', correct: true, why: 'ValidateService is the last hook of the deployment lifecycle and exists to verify the revision, with a non-zero exit failing the deployment.' },
      { id: 'B', text: 'BeforeInstall, so problems are caught as early as possible.', correct: false, why: 'The new revision is not installed yet, so there is nothing to smoke test.' },
      { id: 'C', text: 'ApplicationStop, which runs first on the instance.', correct: false, why: 'It runs from the previously deployed revision to shut the application down cleanly.' },
      { id: 'D', text: 'AfterAllowTraffic, which runs once the deployment completes.', correct: false, why: 'It is a valid place to test, but by then traffic is already flowing — the opposite of the requirement. It also only exists for blue/green and load-balanced deployments.' },
    ],
    explanation:
      'Learn the hook order: ApplicationStop, BeforeInstall, AfterInstall, ApplicationStart, ValidateService, and for blue/green the BeforeAllowTraffic and AfterAllowTraffic pair. Questions are usually decided by which hook sits either side of traffic.',
    source: 'https://docs.aws.amazon.com/codedeploy/latest/userguide/reference-appspec-file-structure-hooks.html',
  },
  {
    id: 'dva-d3-009',
    certs: ['DVA-C02'],
    taskId: 'dva-3.4',
    type: 'single',
    difficulty: 3,
    serviceSlugs: ['ecs', 'codedeploy', 'elb'],
    stem: 'An ECS service must deploy a new task definition to a second target group, run automated tests against it on a port no user traffic uses, then shift production traffic across in one step. What supports this?',
    options: [
      { id: 'A', text: 'A CodeDeploy blue/green deployment for ECS with a test listener on the test port and an AfterAllowTestTraffic hook.', correct: true, why: 'ECS blue/green with CodeDeploy defines a production listener and an optional test listener, and the lifecycle hooks let you validate the replacement task set before shifting traffic.' },
      { id: 'B', text: 'The ECS rolling update deployment type with minimumHealthyPercent set to 100.', correct: false, why: 'Rolling updates replace tasks in the same target group, so there is no separate environment to test in isolation.' },
      { id: 'C', text: 'An ECS service with two target groups and manual deregistration.', correct: false, why: 'It approximates the shape by hand and gives up the automated hooks, rollback and traffic shifting the question describes.' },
      { id: 'D', text: 'A Lambda alias with a canary routing configuration.', correct: false, why: 'Alias traffic shifting is a Lambda feature and does not apply to ECS services.' },
    ],
    explanation:
      'Three deployment types for ECS: rolling update by the ECS service scheduler, blue/green via CodeDeploy, and external. Only blue/green gives you a test listener and pre-traffic validation.',
    source: 'https://docs.aws.amazon.com/AmazonECS/latest/developerguide/deployment-type-blue-green.html',
  },
  {
    id: 'dva-d3-010',
    certs: ['DVA-C02'],
    taskId: 'dva-3.1',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['sam', 'cloudformation', 's3'],
    stem: 'A SAM template references a local directory for function code. Deploying the template directly with CloudFormation fails because CodeUri is not an S3 location. What resolves this correctly?',
    options: [
      { id: 'A', text: 'Run sam build then sam deploy, which packages the code to S3 or ECR and submits the transformed template.', correct: true, why: 'Packaging rewrites local paths into artefact URIs and uploads the bundles. sam deploy does this and then creates a CloudFormation change set.' },
      { id: 'B', text: 'Zip the project by hand and set CodeUri to the local zip file.', correct: false, why: 'CloudFormation cannot read a local path at all, zipped or not.' },
      { id: 'C', text: 'Add the Serverless transform a second time so CloudFormation resolves the path.', correct: false, why: 'The transform expands SAM resource types. It does not upload anything.' },
      { id: 'D', text: 'Inline the function code in the template with the ZipFile property.', correct: false, why: 'Inline code is capped at 4,096 characters and cannot carry dependencies, so it is unusable for real functions.' },
    ],
    explanation:
      'The SAM chain is build, then deploy (package plus create-change-set plus execute). Plain CloudFormation has the same step as aws cloudformation package.',
    source: 'https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/sam-cli-command-reference-sam-deploy.html',
  },
  {
    id: 'dva-d3-011',
    certs: ['DVA-C02'],
    taskId: 'dva-3.1',
    type: 'multi',
    difficulty: 2,
    serviceSlugs: ['ecr', 'ecs'],
    stem: 'A team wants the exact artefact tested in staging to be the artefact that runs in production, and wants old images cleaned up automatically. Which two ECR configurations support this? (Choose two.)',
    options: [
      { id: 'A', text: 'Enable tag immutability on the repository.', correct: true, why: 'Immutable tags stop a tag being repointed at different content, so the thing you tested is provably the thing you deploy.' },
      { id: 'B', text: 'Reference images by digest rather than by a moving tag such as latest.', correct: true, why: 'A digest is a content hash, so it identifies one exact image no matter what tags are added or moved later.' },
      { id: 'C', text: 'Rebuild the image in each environment from the same commit.', correct: false, why: 'Two builds of one commit can differ — base image updates, dependency resolution, build-time timestamps. Build once, promote the artefact.' },
      { id: 'D', text: 'Enable scan on push.', correct: false, why: 'Vulnerability scanning is worth having but says nothing about artefact identity or cleanup.' },
      { id: 'E', text: 'Use one repository per environment and copy images between them on promotion.', correct: false, why: 'A copy creates a new object to verify, and the question asks for the same artefact rather than a duplicate of it.' },
    ],
    explanation:
      'Build once, promote the same immutable artefact, and let a lifecycle policy expire untagged or old images. Rebuilding per environment is the anti-pattern the exam is testing.',
    source: 'https://docs.aws.amazon.com/AmazonECR/latest/userguide/image-tag-mutability.html',
  },
  {
    id: 'dva-d3-012',
    certs: ['DVA-C02'],
    taskId: 'dva-3.2',
    type: 'single',
    difficulty: 1,
    serviceSlugs: ['device-farm'],
    stem: 'Before release, a mobile client must be tested against a range of real physical Android and iOS devices, with video and logs from each run, driven from the CI pipeline. Which service does this?',
    options: [
      { id: 'A', text: 'AWS Device Farm.', correct: true, why: 'Device Farm runs automated and manual tests on real devices in the cloud and returns videos, screenshots, logs and performance data.' },
      { id: 'B', text: 'Amazon EC2 Mac instances running simulators.', correct: false, why: 'Simulators are not real devices and miss hardware, radio and vendor-specific defects, which is the point of the requirement.' },
      { id: 'C', text: 'AWS CodeBuild with a mobile build image.', correct: false, why: 'CodeBuild compiles and runs unit tests. It has no device fleet.' },
      { id: 'D', text: 'Amazon AppStream 2.0.', correct: false, why: 'AppStream streams desktop applications to users. It is not a test service.' },
    ],
    explanation:
      'Device Farm is the only "real devices" answer on either exam. If a question names physical handsets, it is Device Farm.',
    source: 'https://docs.aws.amazon.com/devicefarm/latest/developerguide/welcome.html',
  },
  {
    id: 'dva-d3-013',
    certs: ['DVA-C02'],
    taskId: 'dva-3.4',
    type: 'single',
    difficulty: 3,
    serviceSlugs: ['elastic-beanstalk'],
    stem: 'An Elastic Beanstalk web environment must deploy with no reduction in capacity, no mixing of old and new code on the same instance, and the ability to return to the previous version within seconds if a problem is found. Which deployment approach fits?',
    options: [
      { id: 'A', text: 'Deploy to a second environment and swap CNAMEs — blue/green.', correct: true, why: 'A separate environment means full capacity throughout and no version mixing, and rollback is another CNAME swap, which is the only option that is near-instant.' },
      { id: 'B', text: 'Immutable deployment.', correct: false, why: 'It avoids capacity loss and version mixing by launching fresh instances, but rolling back means another full immutable deployment rather than seconds.' },
      { id: 'C', text: 'Rolling with additional batch.', correct: false, why: 'Capacity is maintained, but old and new versions serve traffic simultaneously during the deployment and rollback is a second rolling deployment.' },
      { id: 'D', text: 'All at once.', correct: false, why: 'It is the fastest and the cheapest, and it takes the whole environment down during the deploy.' },
    ],
    explanation:
      'Rank the Beanstalk policies by what they cost you: all at once (downtime), rolling (reduced capacity), rolling with additional batch (full capacity, mixed versions), immutable (full capacity, no mixing), blue/green (all of that plus instant rollback, at double the environment cost).',
    source: 'https://docs.aws.amazon.com/elasticbeanstalk/latest/dg/using-features.deploy-existing-version.html',
  },
  {
    id: 'dva-d3-014',
    certs: ['DVA-C02'],
    taskId: 'dva-3.4',
    type: 'single',
    difficulty: 3,
    serviceSlugs: ['lambda'],
    stem: 'A team configures provisioned concurrency and points API Gateway at the function ARN without a qualifier. Cold starts continue. What is wrong?',
    options: [
      { id: 'A', text: 'The unqualified ARN resolves to $LATEST, and provisioned concurrency can only be allocated to a published version or an alias — so callers are not using it.', correct: true, why: 'Provisioned concurrency is a property of a version or alias. Invoking $LATEST bypasses it entirely and gets ordinary on-demand concurrency.' },
      { id: 'B', text: 'Provisioned concurrency takes up to 24 hours to become effective.', correct: false, why: 'It is ready within a couple of minutes and reports a Ready status.' },
      { id: 'C', text: 'Reserved concurrency must be set to zero before provisioned concurrency applies.', correct: false, why: 'Reserved concurrency caps and reserves the function ceiling. Setting it to zero would stop invocations altogether.' },
      { id: 'D', text: 'The function must be in a VPC for provisioned concurrency to apply.', correct: false, why: 'VPC configuration is unrelated, and VPC networking is no longer the main cold-start contributor it once was.' },
    ],
    explanation:
      'Versions are immutable, aliases point at versions, and provisioned concurrency, canary traffic shifting and stable integration targets all attach to a version or alias — never to $LATEST.',
    source: 'https://docs.aws.amazon.com/lambda/latest/dg/provisioned-concurrency.html',
  },
  {
    id: 'dva-d3-015',
    certs: ['DVA-C02'],
    taskId: 'dva-3.3',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['cloudformation', 'cloudwatch'],
    stem: 'A CloudFormation stack update must be rolled back automatically if an application error alarm fires during the first ten minutes after the resources are created. What should be configured?',
    options: [
      { id: 'A', text: 'Rollback triggers on the stack update, naming the CloudWatch alarms and a monitoring time in minutes.', correct: true, why: 'Rollback triggers monitor the named alarms for the configured period after resource creation and roll the stack back if any of them fire.' },
      { id: 'B', text: 'A CloudWatch alarm action that calls the CloudFormation DeleteStack API.', correct: false, why: 'Deleting the stack destroys the application rather than restoring the previous version, and an alarm action cannot call arbitrary APIs.' },
      { id: 'C', text: 'A stack policy denying updates to the affected resources.', correct: false, why: 'Stack policies prevent changes during an update. They cannot react to runtime metrics.' },
      { id: 'D', text: 'Termination protection on the stack.', correct: false, why: 'That only prevents accidental stack deletion.' },
    ],
    explanation:
      'CloudFormation rollback triggers are the infrastructure-level equivalent of CodeDeploy alarm rollback: define the alarm that means "this deployment is bad" before you deploy.',
    source: 'https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/using-cfn-rollback-triggers.html',
  },
  {
    id: 'dva-d3-016',
    certs: ['DVA-C02'],
    taskId: 'dva-3.1',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['codeartifact', 'codebuild'],
    stem: 'Builds must consume the team\'s private npm packages alongside public ones from npmjs, from a single registry endpoint, with the public packages cached in the account. What should be configured?',
    options: [
      { id: 'A', text: 'A CodeArtifact repository with an upstream external connection to npmjs, and the build authenticated against it.', correct: true, why: 'An external connection makes CodeArtifact fetch and retain public packages on first request, so one registry serves private and public dependencies and the copies persist even if upstream changes.' },
      { id: 'B', text: 'An S3 bucket of tarballs referenced by URL in package.json.', correct: false, why: 'It works for a handful of packages but abandons versioning, resolution and the registry protocol.' },
      { id: 'C', text: 'A private ECR repository for the packages.', correct: false, why: 'ECR stores OCI container images, not language packages.' },
      { id: 'D', text: 'Two registries configured in .npmrc with a scope mapping for the private one.', correct: false, why: 'It resolves both sources but not from a single endpoint, and nothing is cached in the account.' },
    ],
    explanation:
      'CodeArtifact is the managed package registry for npm, PyPI, Maven, NuGet, Cargo, Ruby, Swift and generic packages. Upstream external connections are how you get a single endpoint plus a durable cache of public dependencies.',
    source: 'https://docs.aws.amazon.com/codeartifact/latest/ug/external-connection.html',
  },
  /* ── Domain 4: Troubleshooting & Optimization (18%) ───────────────────── */
  {
    id: 'dva-d4-001',
    certs: ['DVA-C02'],
    taskId: 'dva-4.2',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['xray'],
    stem: 'A developer needs to search X-Ray traces for a specific customer id to investigate one customer\'s slow requests. How should the id be attached to the trace?',
    options: [
      { id: 'A', text: 'As an annotation, because annotations are indexed and can be used in filter expressions.', correct: true, why: 'Annotations are indexed key–value pairs and are the only thing X-Ray can filter on. This distinction is examined directly.' },
      { id: 'B', text: 'As metadata, which stores arbitrary data on the segment.', correct: false, why: 'Metadata is attached and visible but not indexed, so it cannot be searched or filtered.' },
      { id: 'C', text: 'In the segment name.', correct: false, why: 'Segment names identify the service, and a high-cardinality name would fragment the service map.' },
      { id: 'D', text: 'In a CloudWatch Logs entry correlated by timestamp.', correct: false, why: 'That is manual correlation, not trace filtering, and timestamps are unreliable as a join key.' },
    ],
    explanation:
      'Annotations are indexed and filterable; metadata is not. If a question says "filter traces by X", the answer is an annotation.',
    source: 'https://docs.aws.amazon.com/xray/latest/devguide/xray-concepts.html',
  },
  {
    id: 'dva-d4-002',
    certs: ['DVA-C02'],
    taskId: 'dva-4.1',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['cloudwatch', 'lambda'],
    stem: 'A Lambda function must emit a custom business metric on every invocation with minimal latency and cost. What is the recommended approach?',
    options: [
      { id: 'A', text: 'Write structured logs in the CloudWatch embedded metric format, letting CloudWatch extract the metrics asynchronously.', correct: true, why: 'EMF turns a log line into a metric with no extra API call, so there is no PutMetricData latency on the invocation path and no per-call charge.' },
      { id: 'B', text: 'Call PutMetricData synchronously at the end of the handler.', correct: false, why: 'It adds an API call and its latency to every invocation, and PutMetricData is charged per call.' },
      { id: 'C', text: 'Write the metric to DynamoDB and aggregate it later with a scheduled job.', correct: false, why: 'Reinvents metric collection, with more cost, more code and more delay.' },
      { id: 'D', text: 'Enable Lambda Insights and derive the business metric from it.', correct: false, why: 'Lambda Insights provides system-level telemetry, not your application\'s business metrics.' },
    ],
    explanation:
      'EMF is the intended answer for custom metrics from Lambda: log once, get a metric, pay no per-call API cost.',
    source: 'https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/CloudWatch_Embedded_Metric_Format.html',
  },
  {
    id: 'dva-d4-003',
    certs: ['DVA-C02'],
    taskId: 'dva-4.3',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['lambda'],
    stem: 'A user-facing Lambda function behind API Gateway shows p99 latency of 3 seconds due to cold starts, while p50 is 80 ms. Traffic is steady during business hours. What should be configured?',
    options: [
      { id: 'A', text: 'Provisioned concurrency sized to the business-hours baseline, optionally on a schedule.', correct: true, why: 'Provisioned concurrency keeps environments initialised so requests skip the cold start. Scheduling it to business hours avoids paying for it overnight.' },
      { id: 'B', text: 'Reserved concurrency set to the expected peak.', correct: false, why: 'Reserved concurrency guarantees and caps capacity. It does not pre-warm anything, so cold starts remain.' },
      { id: 'C', text: 'A scheduled EventBridge rule pinging the function every minute to keep it warm.', correct: false, why: 'The old workaround: it warms one environment, not the many needed under concurrency, and it is superseded by provisioned concurrency.' },
      { id: 'D', text: 'Increase the memory allocation to reduce initialisation time.', correct: false, why: 'More memory speeds initialisation somewhat but does not remove the cold start.' },
    ],
    explanation:
      'Cold starts are provisioned concurrency. Concurrency limits are reserved concurrency. The names are similar and the exam exploits that.',
    source: 'https://docs.aws.amazon.com/lambda/latest/dg/provisioned-concurrency.html',
  },
  {
    id: 'dva-d4-004',
    certs: ['DVA-C02'],
    taskId: 'dva-4.1',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['cloudwatch'],
    stem: 'An operations team must be alerted whenever the text "OutOfMemoryError" appears in an application\'s CloudWatch log group. What should be configured?',
    options: [
      { id: 'A', text: 'A metric filter on the log group that increments a custom metric, with a CloudWatch alarm on that metric.', correct: true, why: 'Metric filters turn a log pattern into a metric, and alarms act on metrics. This is the standard log-to-alert path.' },
      { id: 'B', text: 'A CloudWatch alarm directly on the log group.', correct: false, why: 'Alarms evaluate metrics, not log content. A metric filter is the required intermediate step.' },
      { id: 'C', text: 'A Logs Insights query on a schedule, emailing the results.', correct: false, why: 'Possible with extra machinery, but it polls rather than reacting and there is a native mechanism.' },
      { id: 'D', text: 'A CloudTrail trail filtered for the error string.', correct: false, why: 'CloudTrail records API calls, not application log output.' },
    ],
    explanation:
      'Log pattern → metric filter → alarm → SNS. This chain appears repeatedly, in both DVA and SAA framing.',
    source: 'https://docs.aws.amazon.com/AmazonCloudWatch/latest/logs/MonitoringLogData.html',
  },
  {
    id: 'dva-d4-005',
    certs: ['DVA-C02'],
    taskId: 'dva-4.3',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['sns', 'sqs'],
    stem: 'Four SQS queues subscribe to an SNS topic, but each consumer only cares about a subset of message types. Currently every consumer reads and discards most messages. What reduces waste?',
    options: [
      { id: 'A', text: 'Subscription filter policies on each SNS subscription, so only relevant messages reach each queue.', correct: true, why: 'Filtering happens in SNS before delivery, so each queue receives only what it needs — fewer requests, less compute, less cost.' },
      { id: 'B', text: 'A separate SNS topic per message type.', correct: false, why: 'It works but multiplies topics and forces publishers to know the routing, which is what filter policies avoid.' },
      { id: 'C', text: 'A Lambda function between the topic and the queues that routes messages.', correct: false, why: 'Adds a component and its cost to do what SNS does natively.' },
      { id: 'D', text: 'Longer polling intervals on each consumer.', correct: false, why: 'It changes when messages are read, not how many irrelevant ones arrive.' },
    ],
    explanation:
      'DVA names "subscription filter policies to optimize messaging" as a skill. Filter at the source, not in the consumer.',
    source: 'https://docs.aws.amazon.com/sns/latest/dg/sns-message-filtering.html',
  },
  {
    id: 'dva-d4-006',
    certs: ['DVA-C02'],
    taskId: 'dva-4.3',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['cloudfront', 'api-gateway'],
    stem: 'An API returns different responses depending on the Accept-Language header. Behind CloudFront, users are receiving responses in the wrong language. What should be changed?',
    options: [
      { id: 'A', text: 'Include Accept-Language in the CloudFront cache key so responses are cached per language.', correct: true, why: 'A header that changes the response must be part of the cache key, or one language\'s response is served to everyone.' },
      { id: 'B', text: 'Disable caching on the distribution entirely.', correct: false, why: 'It fixes correctness by giving up all caching benefit — an over-correction.' },
      { id: 'C', text: 'Forward all headers to the origin.', correct: false, why: 'It fixes correctness but makes almost every request a cache miss, since the key then includes highly variable headers.' },
      { id: 'D', text: 'Add a Lambda@Edge function to rewrite the response.', correct: false, why: 'Far more machinery than needed for something the cache-key policy expresses directly.' },
    ],
    explanation:
      'Anything that varies the response must be in the cache key — and nothing else should be. That balance is the whole art of CDN caching.',
    source: 'https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/controlling-the-cache-key.html',
  },
  {
    id: 'dva-d4-007',
    certs: ['DVA-C02'],
    taskId: 'dva-4.1',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['api-gateway'],
    stem: 'During a marketing campaign, clients of a REST API begin receiving HTTP 429 with the message "Too Many Requests". Backend Lambda metrics show no errors and low duration. What is the cause?',
    options: [
      { id: 'A', text: 'API Gateway request throttling — the stage, method or usage plan rate and burst limits are being exceeded.', correct: true, why: '429 is the throttling response. With a healthy backend, the limit being hit is in front of it: account, stage, method or usage plan.' },
      { id: 'B', text: 'The Lambda function has reached its reserved concurrency limit.', correct: false, why: 'That also produces throttling, but it surfaces as Lambda Throttles and typically a 500-class response from the integration, and the function metrics here are clean.' },
      { id: 'C', text: 'The integration timeout of 29 seconds is being exceeded.', correct: false, why: 'That returns 504 Gateway Timeout, and duration is reported as low.' },
      { id: 'D', text: 'The Lambda response exceeds the payload limit.', correct: false, why: 'That returns 502 Bad Gateway from a malformed or oversized response.' },
    ],
    explanation:
      'Learn the API Gateway status codes as a diagnostic table: 429 throttling, 502 bad integration response, 504 integration timeout or 29-second limit, 403 authorisation or WAF, 401 missing authentication token.',
    source: 'https://docs.aws.amazon.com/apigateway/latest/developerguide/api-gateway-request-throttling.html',
  },
  {
    id: 'dva-d4-008',
    certs: ['DVA-C02'],
    taskId: 'dva-4.2',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['xray'],
    stem: 'X-Ray costs on a high-traffic API have grown beyond budget, but the team must keep tracing every request to the /checkout path. What should be changed?',
    options: [
      { id: 'A', text: 'Add a custom sampling rule matching the checkout path at a high rate, and lower the default rule\'s reservoir and fixed rate.', correct: true, why: 'Sampling rules are evaluated by priority and match on service, method, URL path and host, so one path can be traced heavily while everything else is sampled sparsely.' },
      { id: 'B', text: 'Disable active tracing on all functions except the checkout function.', correct: false, why: 'It cuts cost but loses the surrounding trace segments, so a checkout trace no longer shows the services it called.' },
      { id: 'C', text: 'Reduce the trace retention period.', correct: false, why: 'X-Ray retains traces for 30 days and this is not configurable, so there is no such lever.' },
      { id: 'D', text: 'Send traces to CloudWatch Logs instead and query them with Logs Insights.', correct: false, why: 'Logs are not traces — you lose the service map and segment timings that made X-Ray useful here.' },
    ],
    explanation:
      'The default sampling rule is one request per second plus 5% of the remainder. Custom rules with a lower priority number win, which is how you trace a critical path heavily and the rest cheaply.',
    source: 'https://docs.aws.amazon.com/xray/latest/devguide/xray-console-sampling.html',
  },
  {
    id: 'dva-d4-009',
    certs: ['DVA-C02'],
    taskId: 'dva-4.2',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['cloudwatch', 'lambda'],
    stem: 'An investigation needs the p95 duration of one downstream call, broken down by customer tier, across a week of Lambda logs. The team controls the logging code. What is the most effective preparation?',
    options: [
      { id: 'A', text: 'Emit one structured JSON log line per call with duration and tier as fields, and query it with CloudWatch Logs Insights using filter, stats and pct.', correct: true, why: 'Logs Insights discovers fields in JSON automatically, so structured logging turns an ad-hoc text search into an aggregation query with percentiles and grouping.' },
      { id: 'B', text: 'Log a human-readable sentence per call and grep the exported log files.', correct: false, why: 'Unstructured text has to be re-parsed for every new question, and exporting a week of logs to work on them locally is slow and error-prone.' },
      { id: 'C', text: 'Publish a custom CloudWatch metric per customer, dimensioned by customer id.', correct: false, why: 'It gives fast percentiles but a unique dimension value per customer creates a custom metric per customer, which is expensive and hits cardinality limits.' },
      { id: 'D', text: 'Enable X-Ray active tracing and read the durations from the service map.', correct: false, why: 'Tracing is excellent for one slow request, but sampled traces cannot give a reliable p95 over a week, and the service map does not group by a business attribute.' },
    ],
    explanation:
      'Structured logs are queryable data. The rule of thumb: metrics for known questions at low cardinality, logs for unknown questions at high cardinality, traces for one request end to end.',
    source: 'https://docs.aws.amazon.com/AmazonCloudWatch/latest/logs/CWL_QuerySyntax.html',
  },
  {
    id: 'dva-d4-010',
    certs: ['DVA-C02'],
    taskId: 'dva-4.3',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['dynamodb', 'elasticache'],
    stem: 'A read-heavy DynamoDB workload needs microsecond response times for repeated reads of the same items, with the smallest possible change to application code. What should be added?',
    options: [
      { id: 'A', text: 'DynamoDB Accelerator (DAX), which the existing DynamoDB API calls can use through the DAX client.', correct: true, why: 'DAX is a write-through cache purpose-built for DynamoDB, delivers microsecond reads and is API-compatible, so the change is the client and endpoint rather than the data-access logic.' },
      { id: 'B', text: 'ElastiCache for Redis in front of the table.', correct: false, why: 'It reaches similar latency but you must write and maintain all the cache-aside and invalidation logic yourself, which is the code change being avoided.' },
      { id: 'C', text: 'Global tables in a second Region.', correct: false, why: 'Global tables address geographic latency and regional resilience, not repeated reads of hot items.' },
      { id: 'D', text: 'Switching every read to eventually consistent.', correct: false, why: 'It halves read cost and helps a little with latency, but single-digit milliseconds is not microseconds.' },
    ],
    explanation:
      'DAX for DynamoDB-only caching with no code rewrite — note it does not accelerate strongly consistent reads. ElastiCache when you need to cache anything else, or need data structures Redis provides.',
    source: 'https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/DAX.html',
  },
  {
    id: 'dva-d4-011',
    certs: ['DVA-C02'],
    taskId: 'dva-4.1',
    type: 'single',
    difficulty: 2,
    serviceSlugs: ['cloudtrail', 's3'],
    stem: 'An object disappeared from an S3 bucket overnight and the team must identify which principal deleted it. CloudTrail is enabled with default settings. What is true?',
    options: [
      { id: 'A', text: 'The DeleteObject call will not be in the trail — object-level activity requires data events, which are not logged by default.', correct: true, why: 'Management events are logged by default; S3 object-level calls are data events and must be enabled explicitly, at additional cost.' },
      { id: 'B', text: 'The call is in the default management event history and can be found by filtering on DeleteObject.', correct: false, why: 'Management events cover control-plane actions such as PutBucketPolicy, not per-object reads and writes.' },
      { id: 'C', text: 'S3 server access logs are enabled by default and will name the principal.', correct: false, why: 'Server access logging is off until configured, and it is a separate feature from CloudTrail.' },
      { id: 'D', text: 'The information is available from the bucket\'s CloudWatch metrics.', correct: false, why: 'Metrics give counts and volumes. They never identify a caller.' },
    ],
    explanation:
      'Management events versus data events is a recurring exam distinction. If a question needs per-object or per-item attribution — S3 objects, Lambda invocations, DynamoDB items — data events must have been switched on beforehand.',
    source: 'https://docs.aws.amazon.com/awscloudtrail/latest/userguide/logging-data-events-with-cloudtrail.html',
  },
  {
    id: 'dva-d4-012',
    certs: ['DVA-C02'],
    taskId: 'dva-4.3',
    type: 'single',
    difficulty: 3,
    serviceSlugs: ['lambda', 'cloudwatch'],
    stem: 'A CPU-bound Lambda function is configured with 512 MB and runs for 8 seconds. A developer proposes raising memory to 2,048 MB to cut cost. Is that plausible, and how should it be decided?',
    options: [
      { id: 'A', text: 'Yes — CPU scales with the memory setting, so a faster run can cost less despite the higher per-millisecond price. Measure across memory settings and compare cost, rather than assuming.', correct: true, why: 'Lambda allocates CPU proportionally to memory, so a CPU-bound function can finish more than four times faster at four times the memory and end up cheaper. Only measurement tells you where the curve bottoms out.' },
      { id: 'B', text: 'No — cost is duration multiplied by memory, so more memory is always more expensive.', correct: false, why: 'That is true only when duration does not improve, which is exactly what does not hold for CPU-bound work.' },
      { id: 'C', text: 'Yes, and 10,240 MB will always be cheapest for CPU-bound functions.', correct: false, why: 'Speed-up flattens once the function stops being CPU-starved, and beyond that point the extra memory is pure cost. There is no universal setting.' },
      { id: 'D', text: 'It cannot be evaluated without moving the workload to Fargate for comparison.', correct: false, why: 'The comparison is a handful of invocations per memory setting, measured from the billed duration in the logs.' },
    ],
    explanation:
      'One vCPU arrives at about 1,769 MB. Right-sizing is an empirical exercise — AWS Lambda Power Tuning is the standard tool, and the answer is often more memory for less money.',
    source: 'https://docs.aws.amazon.com/lambda/latest/dg/configuration-function-common.html',
  },
]
